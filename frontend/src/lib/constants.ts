/**
 * The empty string constant.
 */
export const EMPTY = '';

/**
 * The sign to represent an empty value in the UI.
 */
export const EMPTY_SIGN = '--';

/**
 * The tag to mark the start and end of a loop node.
 */
export const LOOP_TAG = '$loop';
